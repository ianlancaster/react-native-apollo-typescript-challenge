extend type Query {
  AddReservationScreen: AddReservationScreenState!
}

extend type Mutation {
  updateNewReservation(target: String, text: String): Reservation!
  resetNewReservation: Reservation!
}

type AddReservationScreenState {
  newReservation: Reservation!
}
