import { StyleSheet } from 'react-native'
import { accentBackground } from 'styles/colors'

const styles = StyleSheet.create({
  loader: {
    height: '100%',
    backgroundColor: accentBackground,
    justifyContent: 'center',
    alignItems: 'center',
  },
})

export default styles
